services:
  - type: web
    name: motogeo-pro
    runtime: python
    plan: free
    buildCommand: |
      pnpm install --frozen-lockfile
      pnpm run build
    startCommand: python backend/server.py
