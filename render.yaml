services:
  - type: web
    name: motogeo-pro
    runtime: python
    plan: free
    buildCommand: |
      corepack enable
      pnpm install --frozen-lockfile
      pnpm run build
    startCommand: python backend/server.py
